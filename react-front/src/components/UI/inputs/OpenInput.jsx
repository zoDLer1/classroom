import React from 'react'
import Input from './Input'
import { useOpen } from 'hooks/globalUIContent/useOpen'

export default function OpenInput({ condition }) {

    

    return (
        <div>{condition+''}</div>
    )
}
